# SQLite Migration (Not Required)

The application now uses SQLite as the primary datastore and starts from a fresh database. No legacy data migration is required.

For schema details, see:
- [DATABASE_SCHEMA.md](DATABASE_SCHEMA.md)
